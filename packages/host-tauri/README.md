# @bergwork/host-tauri

Host implementation shared by both apps. It implements the image editor's capability contract and, once it
exists, the PDF editor's (see `../../docs/PDF-HOST-CONTRACT.md`): native file dialogs and file associations,
bundled models and OCR, system fonts, OS codecs, printing. Nothing here yet.
